#include <datagenerator.h>
//this module includes the randchar function, which returns a random
//ASCII alphabetic character
//

/* randalpha returns a random alphabetic character */
char randalpha(void) {
  int icase = 0;
  which_case = randint(2);
  
  
   
   

}
